# 📱 모바일 전수 점검 결과

- **대상**: `night-work` 브랜치, 실제 구동 중인 서버(영상 7,418건, 실데이터)
- **방법**: 헤드리스 Chrome 을 CDP 로 몰아 폭별로 **DOM 을 실측**했습니다.
  눈으로 보고 넘긴 게 아니라 요소 좌표·크기를 재서 판정했습니다.
- **폭**: 375 / 390 / 768 (+ 회귀 확인용 1024)
- **탭**: 발굴 · 모아보기 · 신작 · 주간 · 추적 중 · 감시 관리 (6개 전부)

## 판정 기준

| 검사 | 방법 |
|---|---|
| 가로 스크롤 | `documentElement.scrollWidth − innerWidth > 1` |
| 요소 삐져나감 | `#app` 하위 모든 요소의 `getBoundingClientRect()` 가 뷰포트 밖 (가로 스크롤 컨테이너 내부는 제외) |
| 겹침 | 같은 `.row` 안 형제끼리 사각형 교집합이 가로·세로 모두 2px 초과 |
| 터치 영역 | `button`/`select`/`input` 높이 44px 미만 (체크박스는 감싼 `label` 크기로 판정) |
| 제목 짜부라짐 | `.row .title` 폭 90px 미만 |

---

## 결과

### 수정 전 (375px)

| 탭 | 결과 |
|---|---|
| 발굴 | ⚠ 44px 미만 터치 영역 170개 |
| 모아보기 | ⚠ 29개 |
| 신작 | ⚠ 321개 |
| 주간 | ⚠ 121개 |
| 추적 중 | ⚠ 181개 |
| 감시 관리 | ⚠ 264개 |

가로 스크롤·겹침은 없었지만 **썸네일이 제목과 분리돼 한 줄을 통째로 차지**했고
(형제들이 제각각 줄바꿈), 형식 필터는 `미드(1~3 분)` 처럼 글자가 잘려 접혔습니다.
누르는 것들은 대부분 41px, ★ 는 38×32, 그룹 아이콘은 27×21 이었습니다.

### 수정 후

| 폭 | 발굴 | 모아보기 | 신작 | 주간 | 추적 중 | 감시 관리 |
|---|---|---|---|---|---|---|
| **375** | OK | OK | OK | OK | OK | OK |
| **390** | OK | OK | OK | OK | OK | OK |
| **768** | OK | OK | OK | OK | OK | OK |
| 1024 (회귀) | OK\* | OK\* | OK\* | OK\* | OK\* | OK\* |

\* 1024px 에서는 44px 규칙을 적용하지 않습니다(마우스 환경). 가로 스크롤·겹침·짜부라짐은 없습니다.

전 폭·전 탭에서 **가로 스크롤 0 · 겹침 0 · 삐져나감 0 · 제목 짜부라짐 0**,
375/390/768 에서 **44px 미만 터치 영역 0개**입니다.

---

## 고친 것

### 1. 영상 행 2줄 레이아웃 (≤640px)

지표·버튼을 `.tools` 한 덩어리로 묶고, 행을 flex 대신 **grid** 로 바꿨습니다.

```
1줄: [썸네일 92×52] [제목 2줄 + 채널·조회수·날짜]
2줄: [지표 뱃지 ─ 가로 스크롤 ─] [💡] [★] [🔎]
```

묶어 두지 않으면 형제들이 각자 줄바꿈해 **썸네일만 한 줄을 차지**합니다.
`.meta` 는 `flex: 1 1 0; min-width: 0` 이 아니면 긴 제목이 basis 를 밀어올려
자기 줄로 튕겨 나갑니다 — 수정 전 화면이 정확히 그 증상이었습니다.

### 2. 지표 뱃지 = 가로 스크롤 스트립

세로로 3~4줄씩 쌓이던 뱃지를 한 줄 스크롤로 바꿨습니다(스크롤바 숨김).
행 높이가 영상마다 들쭉날쭉하지 않아 훑기 좋습니다.

### 3. 버튼은 아이콘만 (≤768px)

`아이디어로` → 💡, `관련` → 🔎 로 줄이고 라벨은 `title` 속성에 남겼습니다.
44px 로 키운 버튼이 제목 자리를 잡아먹는 걸 막기 위한 교환입니다.
768px 에서 이 처리를 안 했더니 제목이 **74px** 로 짜부라졌습니다(중간 점검에서 발견).

### 4. 필터 줄 가로 스크롤

형식/그룹/채널/구독자를 `.fstrip` 으로 묶어 가로 스크롤합니다.
**건수와 `⬇️ CSV` 버튼은 스트립 밖에 남아** 항상 눌리는 자리에 있습니다.

### 5. 표처럼 늘어놓던 것 → 카드

감시 관리의 그룹·채널·키워드 줄(`.row:not(.row-v)`)을 모바일에서 카드로 바꿨습니다.
흰 배경 + 테두리 + 라운드, 버튼은 가로로 균등 분할.
카테고리 체크박스는 2열 그리드 대신 세로 목록(라벨 전체가 44px 터치 영역).

### 6. 터치 영역 44px (≤768px)

버튼 · 탭 · select · input · 칩 · 그룹 아이콘 · ✕ · 형식 오버라이드 select 전부.
체크박스는 입력 자체를 키우지 않고 **감싼 라벨을 44px** 로 만들었습니다.

---

## 브레이크포인트

| 폭 | 무엇이 바뀌나 |
|---|---|
| ~640px | 영상 행 2줄 grid, 필터 가로 스크롤, 카드형, 버튼 아이콘만 |
| ~768px | 터치 영역 44px, 버튼 라벨 숨김, 지표 폭 260px 상한 |
| 769px~ | 기존 데스크톱 레이아웃 그대로 (변경 없음) |

탭 줄은 폭과 무관하게 항상 가로 스크롤입니다(탭이 6개라 좁은 폭에서 접히면 위계가 흔들립니다).

---

## 남은 것 / 알아 둘 것

- **실기기 확인은 못 했습니다.** 헤드리스 Chrome 의 모바일 에뮬레이션(375×812,
  `mobile: true`) 기준이라, iOS Safari 의 주소창 높이 변화나 관성 스크롤은 확인 범위 밖입니다.
- 지표 뱃지 스트립은 **스크롤 힌트가 없습니다.** 화살표나 그라데이션을 넣을 수 있지만
  지금 팔레트에 군더더기가 붙어 보여 넣지 않았습니다.
- `-webkit-line-clamp` 로 제목을 2줄 자릅니다. 오래된 브라우저에서는 말줄임이 안 될 수 있으나
  레이아웃이 깨지지는 않습니다.
- 계측 도구는 임시 스크립트라 저장소에 넣지 않았습니다(`package.json` 에 테스트 러너가 없어
  구조를 늘리지 않았습니다). 다시 돌리려면 CDP 로 붙는 스크립트를 다시 만들면 됩니다.
