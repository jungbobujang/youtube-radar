# 🌙 밤샘 작업 리포트

- **브랜치**: `night-work` (main 은 건드리지 않았습니다)
- **날짜**: 2026-08-17
- **커밋**: 9개 · 5개 파일 · +940 / −75 줄
- **함께 볼 것**: 건너뛴 것은 `SKIPPED.md`, 실행할 SQL 은 `TODO-SQL.md`

---

## 한눈에

| # | 작업 | 상태 |
|---|---|---|
| 1 | 지표 3종 (에버그린·토론성·포화도) | ⏭️ 이미 구현됨 — 건너뜀 |
| 2 | 대시보드 모바일 최적화 | ✅ |
| 3 | 발굴 탭 CSV 내보내기 | ✅ |
| 4 | 주간 리포트 (`/api/weekly` + 📊 주간 탭) | ✅ |
| 5 | 에러 복원력 + 로컬 실패 로그 | ✅ |
| 6 | README 갱신 | ✅ |
| 7 | 지표 뱃지 시각 정리 | ✅ (11번이 기준을 대체) |
| 8 | 데이터 밀도 개선 | ✅ |
| 9 | 로딩·빈 상태 | ✅ |
| 10 | 상태바 | ✅ |
| 11 | 분포 기준 등급 (Great/Normal/Low) | ✅ |
| 12 | 주간 구독 증가 | ✅ 코드 완료 · **SQL 실행 대기** |

**아침에 할 일 두 가지**

1. `TODO-SQL.md` 의 SQL 한 덩어리를 Supabase 에서 Run (주간 구독 증가용)
2. `npm run dev` 로 띄워 발굴·주간 탭과 상단 상태바 눈으로 확인

---

## 커밋

```
adc6b64 feat: 구독자 일별 이력 + 주간 구독 증가 표시
b6f602d feat: 배율·침투·참여에 분포 기준 등급 병기 (Great/Normal/Low)
acb8ae9 feat: 대시보드 폴리싱 — 상태바·밀도·로딩/빈 상태
3427ac1 docs: README 를 실제 동작 기준으로 갱신
3e509c5 feat: 수집 에러 복원력 + 로컬 실패 로그
da54c28 feat: 주간 리포트 (/api/weekly + 📊 주간 탭)
4f5332e feat: 발굴 탭 CSV 내보내기
a51b58e feat: 대시보드 모바일 최적화
```

---

## 작업별 내용

### 1. 지표 3종 — 건너뜀

에버그린·토론성·포화도 모두 서버·화면·문서까지 이미 완성 상태였습니다.
자세한 확인 근거는 `SKIPPED.md` 1번에 있습니다. **코드를 손대지 않았습니다.**

### 2. 모바일 최적화

폰 폭에서 한 줄에 썸네일·제목·지표·버튼이 다 들어가지 않아 제목이 뭉개지던 문제.

- 지표 뱃지를 통째로 다음 줄로 내리고(`flex: 1 0 100%`), 제목은 두 줄까지 표시
- 탭은 줄바꿈 대신 **가로 스크롤** (스크롤바 숨김)
- 버튼·탭·★·칩의 터치 영역을 40px 안팎으로 확대
- 필터 select 가 세로로 쌓여도 최소 140px 유지
- 기준폭 `max-width: 640px`

### 3. CSV 내보내기

발굴 탭 필터 줄에 `⬇️ CSV`. **서버를 다시 부르지 않고** 화면에 그려진 목록 그대로 내보냅니다(할당량 0).

- 열: 제목 · 채널 · 조회수 · 배율 · 침투 · 참여 · 게시일 · URL
- 엑셀 한글 깨짐 방지 BOM, 쉼표·따옴표·줄바꿈 이스케이프
- `=`·`+`·`-`·`@` 로 시작하는 제목은 작은따옴표를 덧대 **수식 실행 차단**
- 파일명 `youtube-radar-dig-YYYY-MM-DD.csv`

### 4. 주간 리포트

`GET /api/weekly` + 📊 주간 탭. **cron 에 걸지 않았습니다** — 탭을 열 때만 계산하고 DB 만 읽습니다.

| 항목 | 기준 |
|---|---|
| 신작 성과 상위 10 | 감시 채널의 최근 7일 **게시분**, 배율순(동률이면 조회수) |
| 새로 발굴된 배율 5배+ | 최근 7일 **`first_seen_at`** 기준 (발굴 대상은 과거 영상이라 "이번 주에 눈에 띈" 것) |
| 즐겨찾기 | 전체 수 + 이번 주 추가분 |

상수는 `METRIC.WEEKLY_TOP` / `WEEKLY_DIG_MIN` / `WEEKLY_DIG_MAX`.

### 5. 에러 복원력

**점검 결과**: 단계별 try/catch 는 이미 있어 한 곳이 실패해도 다음 단계로 넘어갔습니다.
다만 **할당량이 바닥나면** 감시 대상마다 같은 실패를 반복하며 시간만 태웠습니다.

**보강한 것**

- `YtError(status, reason)` — 응답 본문에서 `quotaExceeded` 등 사유를 파싱
- 할당량(403 quota류 / 429) → **그 자리에서 사이클 종료**, `report.aborted='quota'`,
  다음 cron 에서 정상 복귀. 화면 상태바와 감시 관리 줄에 표시
- 5xx·네트워크 오류만 **3회 재시도**(0.8s → 1.6s). 4xx 는 즉시 포기
- 수동 수집과 cron 이 겹치면 뒤 호출을 **409 로 거절** (중복 실행 = 할당량 이중 소모)
- 예상 못 한 오류도 `collect()` 안에서 삼켜 서버·cron 은 계속 삶

**실패 로그는 DB 가 아니라 파일로**: `logs/collect-YYYY-MM-DD.log` 에 한 줄 JSON
(`at` / `scope` / `status` / `reason` / `message`). `logs/` 는 `.gitignore` 에 넣었습니다.

### 6. README

할당량 설명이 옛 검색 방식(감시 1건당 100유닛) 그대로여서 실제와 달랐습니다. 실측으로 다시 썼습니다.

- 4장: 호출별 단가 + 1회 실측 302유닛(하루 8회 ≈ 2,416유닛),
  **위험한 쪽은 채널 수가 아니라 키워드 개수**(1개 = 하루 808유닛)임을 명시
- 7장 신설: 배율·침투·참여·발굴점수·채널활력·에버그린·토론성·포화도 **공식**과
  `METRIC` 상수 표, 배율에 로그를 씌운 이유, 등급 규칙, 상태바, 주간 리포트 기준
- 8장 신설: 그룹 **다대다** 구조, 필터 4종(그룹·채널·구독자·형식), 주간 구독 증가
- 엔드포인트 표에 누락돼 있던 dig/fresh/weekly/status/starred/related/groups 보강

### 7·8. 뱃지 정리와 데이터 밀도

- 뱃지 색을 **3단계 톤**으로 통일: 기본(회색) / `m-good`(초록) / `m-warn`(주황).
  💬 진한반응·🗣️ 토론형은 "많고 적음"이 아니라 성격이 다른 신호라 별색 유지
- 숫자 포맷 통일: 조회수를 **천 단위 축약**(원래 숫자는 `title` 로 유지),
  배수는 소수 1자리(100 이상은 축약)
- 행 최소 높이를 썸네일에 맞춰 고정, 지표 열 폭 296px 고정 → 뱃지가 세로로 정렬
- 타이포 위계: **제목(14.5px/600) > 채널(12px/600 진회색) > 메타(12px 연회색)**, 행 hover 추가

### 9. 로딩·빈 상태

- `불러오는 중...` 텍스트 → **스켈레톤 행** (썸네일·제목·지표 자리를 그대로 잡아 레이아웃이 튀지 않음)
- 0건이고 필터가 걸려 있으면 **[필터 초기화]** 버튼
- 오류 시 **[다시 시도]** 버튼
- 수집 중 표시는 상태바가 담당 (버튼을 누른 즉시 그리고, 3초 뒤부터 서버 상태로 갱신)

### 10. 상태바

`GET /api/status` 신설. 상단 한 줄에 **마지막 수집 시각 · 총 영상 수 · 오늘 쓴 할당량**.

- 사용률 막대: 70% 주황, 90% 빨강
- 수집 중이면 점멸 표시 + 5초 간격 갱신 (평소 60초)
- 할당량 초과 중단·최근 오류 건수도 여기서 바로 보입니다
- 오늘 쓴 유닛은 **DB 가 아니라** `logs/units-YYYY-MM-DD.json` 에 적습니다.
  할당량 리셋이 **태평양 자정** 기준이라 파일도 그 날짜로 끊습니다
- 합산 대상: 수집 · 채널 갱신 · `유튜브 전체에서 찾기` · 채널 추가(검색 폴백)
- ⚠️ YouTube 가 세는 값이 아니라 **이 앱이 센 값**이라 참고치입니다

### 11. 분포 기준 등급 (뷰트랩)

배율·침투·참여 숫자 옆에 **Great / Normal / Low** 를 색 글자로 병기.

- 상위 25%(≥p75) Great 초록 · 중간 Normal 회색 · 하위 25%(≤p25) Low 연빨강
- 절대 문턱값 대신 **분포**로 판정합니다. 채널 성격에 따라 고정 기준은 금방 무의미해져서입니다
- 세 지표는 축이 다르므로 **각자의 분포로 따로** 판정
- 등급은 화면 80건이 아니라 **필터를 통과한 후보 전체**로 계산 (80건만 보면 분포가 뭉갬)
- 표본 8건 미만이면 등급을 비우고 숫자만 표시
- 적용: 💎 발굴 · 📡 신작. ⭐ 모아보기·📊 주간은 분포가 의미 없어 제외
- 7번의 고정 문턱값 계획은 이 규칙으로 **대체**했습니다 (두 체계가 싸우지 않게)

### 12. 주간 구독 증가 (뷰트랩) — SQL 실행 대기

`yt_channels` 에는 구독자 최신값만 남고 덮어써지므로 이력을 따로 쌓습니다.

- `refreshChannels()` 가 구독자를 갱신할 때 `yt_channel_snapshots` 에 함께 기록
- **하루 1건**: 같은 날(UTC) 기록이 있으면 건너뛰어 강제 갱신을 눌러도 늘지 않음
- 최근 8일치로 채널별 증가량을 계산해 `/api/watches` 에 실어 보냄
- `⚙️ 감시 관리` 채널 줄에 `주간 구독 +1.2천` 칩 (증가 초록 / 감소 주황),
  6일치가 안 차면 `주간 구독 측정 중`
- 이미 부르던 `channels.list` 응답을 저장할 뿐이라 **할당량 추가 소모 없음**
- **테이블 생성 SQL 은 실행하지 않고 `TODO-SQL.md` 에 적어 뒀습니다.**
  실행 전에도 서버는 정상이고 칩만 안 보입니다

---

## 검증

**서버는 띄우지 않았습니다.** `app.listen()` 안에서 곧바로 `collect()` 가 돌아
실제 할당량을 쓰고 운영 DB 에 씁니다 (자세한 판단 근거는 `SKIPPED.md` 3번).

| 방법 | 결과 |
|---|---|
| `node --check server.js` | 커밋마다 통과 |
| 인라인 `<script>` 추출 후 `node --check` | 커밋마다 통과 (HTML 은 `node --check` 대상이 아님) |
| CSV 이스케이프 테스트 | 7/7 통과 (따옴표·쉼표·수식 주입·빈 값) |
| 재시도·할당량 판정 테스트 | 10/10 통과 (5xx 3회, 할당량 즉시 포기, 네트워크 복구, 4xx 즉시 포기, 429) |
| 실패 로그 테스트 | 5/5 통과 (디렉터리 자동 생성, 필드, ISO 시각) |
| 등급 사분위 테스트 | 9/9 통과 (상·하위 25%, 표본 부족, 결측값, 지표별 독립) |
| 주간 구독 증가 테스트 | 5/5 통과 (7일치 증가·감소, 측정 중, 테이블 없음) |

테스트는 가짜 `fetch`/`supabase` 를 주입해 순수 로직만 확인한 것으로,
저장소에는 남기지 않았습니다 (`package.json` 에 테스트 러너가 없어 구조를 늘리지 않았습니다).

---

## 남은 것 / 눈여겨볼 것

1. **`TODO-SQL.md` 실행** — 안 하면 주간 구독 증가만 비어 있습니다
2. **에버그린이 계속 `측정 중`이면** — 백카탈로그 영상은 스냅샷이 1개뿐이라 정상입니다.
   발굴 대상까지 추이를 보려면 수집 때 과거 영상 통계를 다시 읽는 단계가 필요합니다
   (`videos.list` 50개당 1유닛). 새 기능이라 임의로 넣지 않았습니다
3. **오늘 쓴 유닛은 앱이 센 값** — 서버를 껐다 켜도 파일에 남지만 YouTube 실제 집계와
   다를 수 있습니다. 정확한 값은 Google Cloud Console 쪽을 보세요
4. **주간 리포트 자동화** — 지금은 수동 조회만 합니다. cron 등록은 요청대로 하지 않았습니다
