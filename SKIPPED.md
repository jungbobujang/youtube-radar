# SKIPPED — 밤샘 작업에서 건너뛴 것

`night-work` 브랜치 작업 중 **하지 않았거나 확인하지 못한 것**만 모았습니다.
완료 내역은 `NIGHT-REPORT.md` 에 있습니다.

---

## 1. 작업 1 (지표 3종) — 이미 구현되어 있어 건너뜀

지시가 "이미 있으면 건너뛰기" 였고, 확인 결과 셋 다 서버·화면·문서까지 완성 상태였습니다.

| 지표 | 서버 | 화면 | 문서 |
|---|---|---|---|
| 에버그린 (일평균 + 최근 7일 증가량) | `velocityMap()` · `attachEvergreen()` | `evergreenChip()` + `최근 속도순` 정렬 | README 1-j |
| 토론성 (댓글/좋아요 ≥ 0.15) | `deriveMetrics()` 의 `debate` | 🗣️ 토론형 뱃지 | README 1-j |
| 주제 포화도 (최근 90일 타 채널 수) | `saturationMap()` · `attachSaturation()` | `saturationChip()` | README 1-j |

**손대지 않았습니다.** 다만 에버그린·포화도는 DB 함수(`video_velocity`,
`topic_saturation`)가 있어야 값이 찹니다. 발굴 탭에서 계속 `측정 중` 만 보인다면
README 1-j 절의 SQL 이 실행됐는지 확인하세요 (`TODO-SQL.md` 하단에도 정리해 뒀습니다).

---

## 2. DB 스키마 변경 — 규칙대로 실행하지 않음

작업 12(주간 구독 증가)에 `yt_channel_snapshots` 테이블이 필요합니다.
**"DB 변경은 TODO-SQL.md 에만" 이라는 지시에 따라 실행하지 않고 SQL 만 적어 뒀습니다.**

- 실행 전: 감시 관리 화면에 구독 증가 칩만 안 보입니다. 서버 로그에 경고 한 줄.
- 실행 후: 다음 채널 갱신(최대 24시간 뒤)부터 기록이 쌓이고, 7일 뒤부터 숫자가 나옵니다.

---

## 3. 실제 구동 검증 — 하지 않음

**서버를 띄우지 않았습니다.** `app.listen()` 안에서 곧바로 `collect()` 가 돌아
YouTube 할당량을 쓰고 Supabase 에 실제로 씁니다. 무인 야간 작업에서 사용자 확인 없이
외부 API·운영 DB 를 건드리는 건 위험 범위라고 판단했습니다.

대신 이렇게 확인했습니다.

| 방법 | 대상 |
|---|---|
| `node --check server.js` | 커밋마다 |
| 인라인 `<script>` 추출 후 `node --check` | 커밋마다 (HTML 은 `node --check` 가 못 봅니다) |
| 순수 로직 단위 테스트 (가짜 fetch/supabase 주입) | CSV 이스케이프, 재시도·할당량 판정, 실패 로그, 등급 사분위, 주간 구독 증가 |

### 그래서 남은 미검증 항목

- `/api/weekly`, `/api/status` 의 **실제 응답** (Supabase 연결 필요)
- `attachGrades()` 를 실제 데이터에 물렸을 때의 등급 분포 체감
- 모바일 레이아웃 **실기기 확인** (CSS 규칙 검토만 했습니다)
- CSV 파일을 **엑셀에서 연 결과** (BOM·수식 방지는 문자열 단위로만 검증)
- 할당량 초과 상황의 **실제 재현** (403 응답은 가짜 fetch 로만 재현)

아침에 `npm run dev` 로 띄워 발굴/신작/주간 탭과 상태바만 눈으로 확인해 주시면
나머지는 로직 테스트로 덮여 있습니다.

---

## 4. 손대지 않은 것

- `.env` — 지시대로 열지도 고치지도 않았습니다
- `main` 브랜치 — 모든 커밋은 `night-work` 에만 있습니다
- cron 등록 — 주간 리포트는 요청대로 **수동 조회만** 되게 두었습니다
  (`/api/weekly` 는 탭을 열 때만 계산합니다)
- 기존 대시보드 구조 — 폴리싱 지시가 "전면 재디자인 금지" 였으므로
  탭 구성·행 구조·색 팔레트는 그대로 두고 규칙만 정리했습니다
